#ifndef MESH_RESAMPLER_H
#define MESH_RESAMPLER_H

/* Provides global mutation functionality for halfEdge Meshes.
 *
 * Written by Bryce Summers.
 */
#include "halfEdgeMesh.h"

using namespace std;

namespace CMU462 {

   class MeshResampler{

      public:

         MeshResampler(){};
         ~MeshResampler(){}

         void upsample  ( HalfedgeMesh& mesh );
         void downsample( HalfedgeMesh& mesh );
         void resample  ( HalfedgeMesh& mesh );

      private:

   };
}

#endif // MESH_RESAMPLER_H
